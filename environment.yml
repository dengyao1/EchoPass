# 可选：自行 `conda env create -f environment.yml` 时用于带 openssl 等 conda 包。
# 推荐：conda create -n echopass python=3.8 → activate → ./scripts/first-run.sh（Unix）或 .\\scripts\\first-run-windows.ps1（Windows）。
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.8
  - pip
  - openssl
